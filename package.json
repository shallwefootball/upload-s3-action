{
  "name": "cloudflare-r2-upload",
  "description": "Upload directory to Cloudflare R2",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "repository": "https://github.com/okkema/github-action-r2",
  "author": "Okkema Labs <correos@okkema.org>",
  "scripts": {
    "build": "esbuild index.mjs --bundle --outdir=dist --platform=node --format=cjs"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@aws-sdk/client-s3": "^3.379.1",
    "klaw-sync": "^6.0.0",
    "mime-types": "^2.1.35",
    "slash": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}
